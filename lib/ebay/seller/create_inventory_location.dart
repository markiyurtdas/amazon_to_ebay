import 'package:flutter/cupertino.dart';

class CreateInventoryLocation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [],
      ),
    );
  }
}
